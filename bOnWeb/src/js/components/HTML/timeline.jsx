/**
 * @module TimeLine
 */

// React
const React = require('react');
const PageHeader = require('../HTML/layout/PageHeader');

const TimeLine = React.createClass({

	render() {
		return (
			<PageHeader/>

		);
	},
});

module.exports = TimeLine;
