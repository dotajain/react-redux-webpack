/**
 * @module CredentialsConstants
 * @requires keyMirror
 */

const keyMirror = require('keymirror');

/**
 * @const Constants
 */
const Constants = keyMirror({
	GET_CREDENTIALS: null,
});

module.exports = Constants;
